[plugin]
name = "image_memory"
version = "0.1.0"
description_key = "plugin.image_memory.description"
description = "Cross-session image asset and semantic memory"
builtin = true

[entry]
hooks = "hooks:register"
